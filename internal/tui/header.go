package tui

// renderHeader renders the top header bar showing cluster name, status, and timestamps.
// Placeholder — full implementation in Task 6.
func renderHeader(app *App) string {
	return ""
}
